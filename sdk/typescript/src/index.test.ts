import { describe, expect, it } from "vitest"; import { BillingError, GrowXAI } from "./index.js";
describe("GrowXAI", () => { it("constructs authenticated requests and maps billing errors", async () => { const client = new GrowXAI({ apiKey: "key", fetch: async (_url, init) => { expect((init?.headers as Record<string, string>).authorization).toBe("Bearer key"); return new Response(JSON.stringify({ error: { code: "insufficient_credits", message: "Top up", requestId: "req_1" } }), { status: 402, headers: { "content-type": "application/json" } }); } }); await expect(client.responses.create({ model: "growx/smart", input: "hi" })).rejects.toBeInstanceOf(BillingError); }); });
