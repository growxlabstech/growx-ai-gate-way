import { AppShell } from "../../../../../components/app-shell";
export default async function RoutingAnalyticsPage({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string }> }) { const { organizationSlug, workspaceSlug } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Routing analytics"><div className="metric-grid">{[["Requests", "—"], ["P95 latency", "—"], ["Fallback rate", "—"], ["Cache hit rate", "—"], ["Error rate", "—"]].map(([name, value]) => <section className="metric" key={name}><p>{name}</p><strong>{value}</strong></section>)}</div><section className="state"><h2>Routing performance</h2><p>Customer-safe model, latency, fallback, cache, and error trends appear here. Proprietary provider scoring remains internal.</p></section></AppShell>; }
