import { AppShell, StatePanel } from "../../../../components/app-shell";
export default async function Page({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string }> }) { const { organizationSlug, workspaceSlug } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Usage"><div className="filters"><button type="button">Today</button><button type="button">7 days</button><button type="button">30 days</button><button type="button">Current billing period</button><button type="button">Export CSV or JSON</button></div><StatePanel title="No usage in this range" detail="Usage queries are organization and workspace scoped, server filtered, and cursor paginated." /></AppShell>; }
