# Deployment rollback

Stop rollout, record artifact digest and symptoms, verify migration compatibility, route to last-known-good immutable artifact and monitor errors, latency, financial invariants and queue recovery. Never apply destructive schema rollback blindly; use forward-fix/expand-contract strategy. Reconcile emergency manual changes back into code.
