import { AppShell } from "../../../../components/app-shell";
export default async function LogsPage({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string }> }) { const { organizationSlug, workspaceSlug } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Gateway logs"><table className="data-table"><thead><tr><th>Request ID</th><th>Timestamp</th><th>Model</th><th>Status</th><th>Latency</th><th>Tokens</th></tr></thead><tbody><tr><td>req_…</td><td>—</td><td>growx/fast</td><td>Completed</td><td>—</td><td>—</td></tr></tbody></table><p className="muted">Raw prompts and provider credentials are never included in request logs.</p></AppShell>; }
