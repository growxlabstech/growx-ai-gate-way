# Privileged account compromise

Terminate privileged and linked workforce sessions, revoke JIT grants, disable identity, preserve immutable audit, rotate exposed credentials and activate break-glass only through its controlled workflow. Review all privileged reads/writes and support sessions. Security leads containment and post-incident review.
