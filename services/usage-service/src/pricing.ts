import { moneyToCredits, type CreditConversionVersion } from "@growx/credits"; import { calculatePrice, providerTokenCost, type PricingRule } from "@growx/pricing";
export interface UsageUnits { inputTokens: bigint; outputTokens: bigint; cachedInputTokens: bigint; reasoningTokens: bigint; }
export interface ProviderPrice { id: string; currency: string; inputPerMillion: bigint; outputPerMillion: bigint; cachedPerMillion: bigint; reasoningPerMillion: bigint; }
export function priceUsage(units: UsageUnits, provider: ProviderPrice, customer: PricingRule, conversion: CreditConversionVersion) { const providerCost = providerTokenCost({ ...units, cachedTokens: units.cachedInputTokens, reasoningTokens: units.reasoningTokens, inputPerMillion: provider.inputPerMillion, outputPerMillion: provider.outputPerMillion, cachedPerMillion: provider.cachedPerMillion, reasoningPerMillion: provider.reasoningPerMillion, currency: provider.currency }); const price = calculatePrice(providerCost, customer); return { ...price, providerPricingVersionId: provider.id, creditConversionVersionId: conversion.id, creditsConsumed: moneyToCredits(price.customerCharge.amountMinor, conversion) }; }
