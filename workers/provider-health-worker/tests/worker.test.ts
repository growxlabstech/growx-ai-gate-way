import { describe, expect, it } from "vitest"; import { MockAIProvider } from "@growx/testing"; import { checkProviders } from "../src/index.js";
describe("provider health", () => { it("persists and emits health changes", async () => { const events: string[] = []; await checkProviders([{ providerId: "mock", adapter: new MockAIProvider(), baseUrl: "x", credential: "x" }], { async previous() { return "unknown"; }, async save(_id, health) { events.push(health.state); }, async emitChanged() { events.push("changed"); } }, new AbortController().signal); expect(events).toEqual(["healthy", "changed"]); }); });
