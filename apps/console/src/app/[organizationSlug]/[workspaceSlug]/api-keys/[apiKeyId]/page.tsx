import { AppShell, EnvironmentBadge } from "../../../../../components/app-shell";
export default async function ApiKeyDetailPage({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string; apiKeyId: string }> }) { const { organizationSlug, workspaceSlug, apiKeyId } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Production app"><p className="muted">{apiKeyId} · gx_live_•••••••••••• <EnvironmentBadge type="production" /></p><div className="tabs">{["Overview", "Permissions", "Models", "Rate limits", "Spending", "IP restrictions", "Usage", "Audit activity"].map((tab) => <span key={tab}>{tab}</span>)}</div><section className="state"><h2>API key is active</h2><p>Created recently · Never used · Never expires</p><div className="toolbar"><button type="button">Rotate key</button><button type="button">Revoke key</button></div><p className="warning">Applications using a revoked key immediately lose access.</p></section></AppShell>; }
