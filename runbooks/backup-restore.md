# Backup restore

Follow `BACKUP_AND_RESTORE.md`. Restore only to isolated infrastructure with separate credentials, verify checksums/schema/tenant samples/ledger balance, measure RTO/data loss, prevent external egress and record evidence. Never call a backup valid solely because creation succeeded.
