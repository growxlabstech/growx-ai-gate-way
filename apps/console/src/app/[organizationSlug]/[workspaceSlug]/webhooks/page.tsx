import { AppShell, StatePanel } from "../../../../components/app-shell";
export default async function Page({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string }> }) { const { organizationSlug, workspaceSlug } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Webhooks"><div className="header-actions"><button type="button">Create endpoint</button></div><StatePanel title="No webhook endpoints" detail="Endpoints require HTTPS, are signed with HMAC-SHA-256, and never follow redirects automatically." /></AppShell>; }
