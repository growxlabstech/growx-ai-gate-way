import { AppShell } from "../../../../../components/app-shell";
export default async function ModelPage({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string; modelId: string }> }) { const { organizationSlug, workspaceSlug, modelId } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title={decodeURIComponent(modelId)}><div className="tabs">{["Overview", "Capabilities", "Context limits", "Pricing", "Availability", "API usage"].map((value) => <span key={value}>{value}</span>)}</div><section className="state"><h2>Available for this workspace</h2><p>Streaming and tool support are resolved from the versioned model registry at request time.</p><pre>{`curl https://api.growxlabs.tech/v1/responses \\\n  -H "Authorization: Bearer $GROWX_API_KEY" \\\n  -d '{"model":"${decodeURIComponent(modelId)}","input":"Hello"}'`}</pre></section></AppShell>; }
