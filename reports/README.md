# Production evidence reports

Reports are evidence records, not aspirational checklists. `Not run` must never be changed to `Pass` without commands, environment, timestamps, artifact/build identifiers, measurements, failures and reviewer approval.
