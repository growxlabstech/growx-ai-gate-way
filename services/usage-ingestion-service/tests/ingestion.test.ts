import { describe, expect, it } from "vitest"; import { UsageIngestionService, type UsageEvent } from "../src/ingestion.js";
describe("usage ingestion", () => { it("deduplicates event IDs", async () => { const values = new Map<string, UsageEvent>(); const service = new UsageIngestionService({ async hasEvent(id) { return values.has(id); }, async persist(event) { values.set(event.id, event); } }); const event = { id: "evt_a", type: "gateway.request.completed", version: 1, requestId: "req_a", traceId: "trace_a", organizationId: "org_a", workspaceId: "ws_a", environmentId: "env_a", apiKeyId: "key_a", requestedModel: "growx/fast", resolvedModel: "openai/gpt", providerId: "openai", usage: { inputTokens: 1, outputTokens: 2 }, timing: { latencyMs: 10 }, occurredAt: new Date().toISOString() }; expect(await service.ingest(event)).toBe("stored"); expect(await service.ingest(event)).toBe("duplicate"); }); });
