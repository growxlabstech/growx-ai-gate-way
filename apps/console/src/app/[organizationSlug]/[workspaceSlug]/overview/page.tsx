import { AppShell } from "../../../../components/app-shell";
const metrics = [["Requests today", "—"], ["Credits remaining", "—"], ["Current spend", "—"], ["Error rate", "—"], ["Average latency", "—"], ["Active API keys", "—"]];
export default async function Page({ params }: { params: Promise<{ organizationSlug: string; workspaceSlug: string }> }) { const { organizationSlug, workspaceSlug } = await params; return <AppShell organizationSlug={organizationSlug} workspaceSlug={workspaceSlug} title="Workspace overview"><p className="muted">{organizationSlug} / {workspaceSlug} / Development</p><section className="metric-grid">{metrics.map(([label, value]) => <article className="metric" key={label}><span>{label}</span><strong>{value}</strong></article>)}</section><section className="panel"><h2>Get your first request running</h2><ol><li>Create an API key</li><li>Choose a model</li><li>Run a Playground request</li><li>Install an SDK</li></ol><p>Completion is verified from platform activity, not clicks.</p></section><section className="panel"><h2>Recent requests and provider status</h2><p>No request activity yet. Production data is cursor-paginated and loaded server-side.</p></section></AppShell>; }
